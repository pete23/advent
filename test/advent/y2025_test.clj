(ns advent.y2025-test
  (:require [clojure.test :refer :all]))

;; NOTE: 2025 solutions require dependencies not available from Maven Central:
;; - dom-top.core (from Clojars)
;;
;; To run these tests, add to project.clj:
;; [dom-top "1.0.9"]
;;
;; Files present:
;; - day_1.clj, day_2.clj
;; - Resources in resources/y2025/
;;
;; Once dependencies are available, run solutions and add tests here.

;; Example test structure (uncomment when dependencies available):
;;
;; (deftest day-1-test
;;   (require 'advent.y2025.d1)
;;   (is (= expected (advent.y2025.d1/part-1)))
;;   (is (= expected (advent.y2025.d1/part-2))))
